#include <iostream>
using namespace std;

// The C++ compiler used by CSES is g++ 4.8.4. Used flags are -std=c++0x -O2 -Wall.

// The default cin and cout may be too slow as default 
// but they can be made much faster by removing synchronization with C's stdio library. 
// This can be done by adding line
// cin.sync_with_stdio(false);
// in the begin of main function:

int main() {
    cin.sync_with_stdio(false);
    
    // solve the task here
}